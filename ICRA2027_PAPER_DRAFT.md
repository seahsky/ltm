# When Does Long‑Term Memory Help an Embodied Agent? A Module‑Attributed Boundary and Its Instance‑Discrimination Cause

_with audio as a triggering and gating modality for lifelong anomaly response_

**Authors:** [TBD] · **Target venue:** ICRA 2027 · **Status:** DRAFT v0.2 (reframed 2026‑06‑22 per the direction-check in `docs/related-work-research-2026-06-22.md`; corrected wide headline +0.23 from the shuffle‑fix re‑run; numbers RACE-verified unless marked [verify])

---

## Abstract

We study a lifelong hierarchical long‑term memory (LTM) for an embodied agent and ask a question that is usually assumed away: *when* does long‑term memory actually help navigation, and when does it not? We build a four‑module LTM — short‑term buffering, bio‑inspired consolidation with an importance score, a three‑layer FAISS memory, and memory‑injected re‑ranking — on top of a vision‑language navigation backbone (a 2B image captioner and a 7B language planner) and evaluate it in the Habitat simulator on HM3D scenes. We instantiate it in an **audio‑cued anomaly‑response** task: the agent first maps a home in silence, then an anomaly sound (alarm, glass break, baby cry) fires from a source co‑located with an object, and the agent must reach it. We report three results and a mechanism. **(1) Memory helps when past observation is *relevant*:** on warm revisits the LTM improves paired soft‑SPL by **+0.23 (n=26, p<0.001)** — a fix to an episode‑ordering shuffle bug corrected this upward from a diluted +0.115, consistent with the **+0.24 (n=12, p=0.008)** subset — and lifts 1.0 m success to **0.67** (from 0.33); a setting decomposition attributes essentially all of the gain to the LTM (not the short‑term buffer). The effect **reproduces in the audio task** (+0.171, n=18, p=0.002), giving the first significant strict‑radius (0.1 m) success gain we observe — reachable only because the cold seed begins at a goal view‑point. These estimates come from **≈2–3 distinct datasets** (the wide 6‑category matrix, its chair+bed subset, and the audio task); our other reproductions are reruns and re‑analyses of the same arms, not independent confirmations. **(2) Memory is cleanly *neutral* when observation is incidental:** on cold multi‑object chains the LTM produces exactly zero compounding (Progress 0.190 with and without memory). **(3) Every *additional* memory head we add — learned importance, room‑affordance priors, a temporal‑recency head, an audio direction‑of‑arrival re‑ranker, and writing the heard anomaly into memory — is inert or redundant,** and we trace all of these to one bottleneck: the caption embedding cannot discriminate object *instances*, so a single‑goal evaluation never rewards the disambiguation these heads provide. Our contribution is therefore not a new state‑of‑the‑art module but a **rigorously‑bounded account of where embodied LTM helps**, reproduced in a second task family (audio, heterogeneous across cells) with adversarially‑checked ablations, plus a verified open‑set audio anomaly detector and a causal demonstration that the audio onset is *necessary* for the recall. We argue the next gain must come from instance‑level perception or a multi‑goal/changed‑world evaluation — not from more memory heads.

---

## 1. Introduction

A robot deployed in a home for weeks accumulates experience it should be able to reuse: where it saw the kitchen, which room had the crib, where it last heard a window break. This is the promise of **lifelong long‑term memory (LTM)** for embodied agents. Recent vision‑language navigation systems (e.g., ReMEmbR [Anwar et al., ICRA 2025]) caption first‑person observations into a memory and query it at decision time, and a large literature on episodic and semantic memory for agents argues that hierarchy, consolidation, and importance‑weighting should help.

Most of that literature reports *that* memory helps. We instead ask **when**, and we treat the negative cases as first‑class results. We are motivated by a concrete application — **anomaly response**: a home robot silently maps its environment, then an anomalous sound occurs, and the robot must localize and approach it. This setting is attractive because it cleanly separates two regimes that are usually entangled: a **warm** regime, where the agent has *relevant* prior experience of the region the anomaly is in, and a **cold** regime, where its prior experience is merely *incidental*.

We build a four‑module LTM faithful to the bio‑inspired memory proposal — STM → consolidation (importance `I = αR + βU + γN`) → a three‑layer (fine/mid/coarse) FAISS memory → memory‑injected re‑ranking — and run it on the ReMEmbR backbone (a 2B vision‑language captioner and a 7B language planner) in Habitat on HM3D. We render audio offline as binaural room‑impulse responses (SoundSpaces) and convolve it in O(1) at run time, so the simulator stays a standard navigation simulator.

Our findings:

1. **Relevant recall helps.** On warm revisits, the full system improves paired soft‑SPL over a memory‑off baseline by **+0.23** (n=26, corrected for the shuffle bug; +0.24 at n=12), lifts 1.0 m success from **0.33 → 0.67**, and the gain reproduces in the audio task (**+0.171**, pre‑correction). A short‑term‑memory‑only setting captures none of it, so the effect is LTM‑specific. (These derive from ≈2–3 distinct datasets — the wide matrix, its chair+bed subset, and the audio task; the remaining reproductions are reruns/re‑analyses of the same arms, not independent confirmations.)
2. **Incidental observation is neutral.** On cold multi‑object chains, where the agent sees objects only in passing while hunting other goals, the LTM yields **exactly zero** compounding.
3. **Extra memory heads do not move the needle.** Five separately‑motivated additions — a trained importance head, a trained utility/forward‑model head, a CLIP‑grounded room‑affordance prior, a temporal‑recency head, and an audio direction‑of‑arrival re‑ranker — are each *built, correct, and inert*. A sixth, **writing the heard anomaly into memory**, is mechanism‑verified but **redundant with visual recall**. We show all six share one cause: the caption embedding's **instance‑discrimination ceiling**, exposed by a single‑goal evaluation that never requires choosing *which* same‑category object to revisit.

The paper's contribution is the **boundary**: a reproduced, module‑attributed account of the regime where embodied LTM helps, and a mechanistic explanation — backed by adversarially‑verified ablations — of why the obvious extensions do not. As applied contributions we add an **open‑set CLAP anomaly detector** (separates anomaly from benign household sound with zero training, equal‑error‑rate 0.00 on our clips) and a **causal onset‑gate** demonstrating the audio onset is necessary for the recall.

---

## 2. Related Work

Retrieval‑ and graph‑structured memory for navigation is a crowded 2025–26 frontier, so we are explicit about what we add: **not** a novel memory architecture, but a module‑attributed account of *when* such memory helps and *why* its obvious extensions do not. The table positions us against the most relevant prior and concurrent work; every arXiv id below has been confirmed to resolve on arXiv (June 2026). Concurrent 2026 works (TrajRAG, SSMG‑Nav, Sound‑Triggered Mobile Manipulation) are cited by arXiv id; their conference venues are still to confirm.

The two nearest **audio‑task** systems deserve explicit prose differentiation, since a reviewer who knows them may read our audio framing as already‑occupied. **HomeEmergency** [Mullen et al., arXiv:2504.01089] maps a home, hears an emergency sound, and navigates to it using a probabilistic dynamic scene graph with Bayesian audio‑direction fusion; **Sound‑Triggered Mobile Manipulation** [Ju et al., arXiv:2601.21667] turns a heard event into a manipulation response. Neither carries the axis we study: a **persistent, consolidated, cross‑visit long‑term memory and the measured boundary of its utility** (warm‑relevant vs cold‑incidental recall, module‑attributed). We also use HM3D + SoundSpaces rather than ThreeDWorld, and treat audio as a *trigger and gate*, explicitly *not* a localizer.

| Work | Memory type | Task | Persistent cross‑visit LTM? | What we add over it |
|---|---|---|---|---|
| **ReMEmbR** [Anwar et al., ICRA 2025] | Flat caption+pos+time vector DB, LLM‑queried | Long‑horizon spatio‑temporal video‑**QA** (NaVQA) | Within‑scene DB; **not** a closed‑loop nav policy | Port to closed‑loop ObjectNav with memory‑injected waypoint re‑ranking + a controlled S1/S2/S3 memory ablation (ReMEmbR has no SPL eval, no memory ablation) |
| **GOAT‑Bench** [Khanna et al., CVPR 2024; arXiv:2404.06609] | Benchmark (tests explicit/implicit memory) | Lifelong **multi‑goal** (5–10 sub‑goals/ep), HM3D | Within‑episode memory reuse | We test **cross‑visit** warm/cold recall with paired bootstrap CIs; → **justified in §4.1** (multi‑week port; we report SR@1.0 m in GOAT convention) |
| **TrajRAG** [Wang et al., arXiv:2605.01700] | Hierarchical coarse→fine retrieval over past trajectories | Zero‑shot ObjectNav, HM3D/MP3D | Yes (lifelong trajectory accumulation) | Closest method; we add consolidation/importance + the **boundary+bottleneck diagnosis** (it reports *that* it helps, not *when* it stops) |
| **Embodied‑RAG** [2024] | Hierarchical fine→coarse "semantic forest" | Nav + explanation, km‑scale | Yes (lifelong, persistent) | Pre‑empts our fine/mid/coarse novelty; we state ours is **fine‑layer + rerank in practice** (mid empty, coarse never chosen) |
| **MemoNav** [CVPR 2024] | STM/LTM/working memory + learned forgetting | Image‑goal nav | Within‑episode working memory | Prior art for STM/LTM+consolidation; we add the *when‑it‑fails* attribution + the instance‑bottleneck cause |
| **SSMG‑Nav** [Niu et al., arXiv:2603.01813] | Persistent consolidating semantic skeleton graph | Lifelong multimodal ObjectNav, GOAT (0.341 SPL vs VLFM 0.159) | Yes | SOTA bar; we are a **memory‑delta** study on a weaker backbone, not an absolute‑SOTA claim |
| **HOV‑SG** [Werby et al., RSS 2024] | Hierarchical open‑vocab 3D scene graph (floor/room/object) | Language‑grounded nav, multi‑story | Yes (working hierarchy) | Their hierarchy works; ours' mid/coarse are inert — we differentiate honestly |
| **ConceptGraphs** [Gu et al., ICRA 2024] | Per‑object‑**instance** open‑vocab scene graph | Perception + planning | Yes | The field's instance‑grounded answer to our exact bottleneck; we measure what a flat caption index costs (the 0.047 rank gap) |
| **RANa** [Naver Labs, 2025] | Within‑env persistent image DB | ImageNav/Instance‑ImageNav/ObjectNav (+15 SPL) | Yes (within‑scene cross‑episode) | Establishes within‑scene recall as a published regime; we add consolidation + the boundary/bottleneck analysis |
| **Generative Agents** [Park et al., UIST 2023] | Memory stream: recency+importance+relevance | LLM‑agent simulacra | N/A | Source of our `I = αR + βU + γN`; we **cite it as the origin**, not as novel |
| **VLFM** [Yokoyama et al., ICRA 2024; arXiv:2312.03275] | BLIP‑2/CLIP image‑text value map (no LTM) | Zero‑shot ObjectNav, HM3D (SR 52.5 / SPL 30.4 @1.0 m) | No | Near‑identical category‑cosine core; → **justified in §4.2** (controlled same‑backbone delta; VLFM is the absolute bar, not a memory‑delta comparator) |
| **HomeEmergency** [Mullen et al., arXiv:2504.01089] | Probabilistic dynamic scene graph + Bayesian audio‑direction fusion | **Map home → hear anomaly → navigate → VLM‑verify**, ThreeDWorld | No (no cross‑visit consolidated LTM) | The nearest audio task; we differentiate on **persistent consolidated cross‑visit LTM + warm/cold recall** (they have none) and HM3D+SoundSpaces vs TDW |
| **Sound‑Triggered Mobile Manipulation** [Ju et al., arXiv:2601.21667] | Sound event → manipulation response | Sound‑triggered mobile manipulation | No (no persistent memory) | Concurrent audio‑triggered task; we differentiate on **persistent LTM + lifelong warm/cold recall** (navigation, not manipulation) |
| **Semantic Audio‑Visual Nav (SAVN)** [Chen et al., CVPR 2021] | End‑to‑end learned memory | Navigate to a semantic sounding object, recall after silence; SoundSpaces | Within‑episode learned memory | Canonical "sound fires → silent → recall" ancestor; we add an **explicit consolidated LTM** + cross‑visit recall, and do **not** claim to localize sound |
| **AVLMaps** [Huang et al., ISER 2023] | Persistent fused audio‑visual‑language voxel map | Audio **disambiguates same‑category instances** | Yes (static fused map) | Directly pre‑empts "audio disambiguates instances"; our single‑goal eval **never exercises** it (the DOA head is zero‑sum‑inert), which we bound honestly |
| **rliable** [Agarwal et al., NeurIPS 2021; arXiv:2108.13264] | Statistical‑rigor reference (not a system) | Small‑n RL/embodied eval | N/A | We frame our paired‑bootstrap + 90% CI design as in‑spirit; → **IQM + probability‑of‑improvement + power accounting reported in §4** (performance profiles N/A — single task) |

**Anomalous sound detection** (DCASE Task 2; open‑set acoustic novelty) frames the normal‑vs‑anomaly decision; we adopt a zero‑shot contrastive‑prompt variant on a pretrained audio‑text model (CLAP). Our emphasis on *negative, mechanism‑level* results connects to recent calls (rliable) for rigorous ablation and reproducibility in embodied AI. (We omit Mem4Nav, withdrawn under investigation.)

---

## 3. System

### 3.1 Backbone and task

The perception/action backbone is **ReMEmbR**: a 2B vision‑language model (Qwen2‑VL‑2B) captions each keyframe; a 7B language model (Qwen2.5‑7B) plans. The agent acts in Habitat on HM3D `val_mini` scenes. A navmesh point‑goal controller (Habitat's `ShortestPathFollower`) executes the agent's self‑chosen waypoint. The three ablation **settings** are: **S1** memory‑off ReMEmbR — an internal frontier‑and‑caption baseline (backbone only, no persistent memory; *not* a re‑implementation of an external system such as VLFM); **S2** short‑term‑memory only; **S3** full LTM system.

### 3.2 STM → consolidation → hierarchical LTM

Keyframes are buffered in a short‑term store, then **consolidated** into the LTM by an importance score `I = αR + βU + γN` (relevance, utility, novelty) that gates the top‑*k* writes. The LTM has three FAISS layers — **fine** (individual sightings), **mid** (clustered patterns), **coarse** (category→region priors). Critically, the fine layer is indexed on the **SBERT text embedding of the VLM caption** (not a raw image embedding): we found the image–text cosine flat and non‑discriminative, while the rich VLM caption makes goal‑vs‑caption similarity discriminative. *In the runs reported here, the mid layer is empty and retrieval queries the fine layer only; the coarse layer is seeded but, as §5.5 shows, never chosen. The measured memory effect is therefore "fine layer + memory‑injected re‑ranking," which we state plainly rather than as a working three‑layer hierarchy.*

### 3.3 Memory‑injected re‑ranking

At each decision the planner proposes frontier waypoints; the bridge **injects** LTM‑derived waypoints (the world positions of past sightings that match the goal query `"there is a {category}"` above a cosine gate, scene‑filtered and de‑duplicated against frontiers) into the candidate pool, and a physics‑aware re‑ranker scores the merged pool. Memory is thus *in the action loop*, not merely a re‑ranking of the planner's own list.

### 3.4 Audio channel

Audio is rendered **offline** as a grid of binaural room‑impulse responses (SoundSpaces) at the anomaly source; at run time the agent's pose indexes the nearest cell and the source clip is convolved in **O(1)** (`fftconvolve`). This two‑environment split keeps Habitat a standard navigation simulator. Three audio components:

- **Onset detection.** A calibrated RMS threshold marks the first step the anomaly is audible (calibrated so the source is audible at ≈4 m, not only point‑blank).
- **Open‑set anomaly detection (Step 1).** A pretrained audio‑text model (CLAP) scores the heard clip against an anomaly‑prompt bank *and* a normal/background‑prompt bank; it fires only when the best anomaly cosine beats the best normal cosine by a calibrated margin. This is a real normal‑vs‑anomaly decision (the prior system force‑classified any loud sound into one of three anomaly classes).
- **Onset‑gate.** When enabled, memory retrieval is suppressed until the anomaly is heard, making the audio onset **causally necessary** for warm recall.

---

## 4. Experimental Setup

**Tasks.** (i) **ObjectNav revisit** — the agent maps a scene (cold pass), then revisits it for a goal it has seen (warm pass); the controlled‑start dataset places warm starts at navigable, same‑category‑reachable poses. (ii) **AudioGoal anomaly response** — the cold pass is silent; on the warm pass an anomaly fires from a source co‑located with a captioned object, and the agent must reach it. (iii) **MultiON** — chains of K=3 object goals per episode, to test whether *incidental* cold sightings compound.

**Metrics.** Primary: **soft‑SPL** (graded path efficiency), reported as a warm **paired** S3−S1 delta with a 90% bootstrap CI. Secondary: **success rate at 1.0 m** (the benchmark radius) and **at 0.1 m** (a strict, localization‑bound radius), `min` distance‑to‑goal, and memory‑activity counters. The **module decomposition** S2−S1 (short‑term only) and S3−S2 (consolidation + LTM + re‑rank) attributes the effect.

**Why soft‑SPL is primary.** Strict success at 0.1 m is perception‑bound: caption‑grounded detection cannot localize a stop to 0.1 m, so binary SPL@0.1 m is near‑zero everywhere except where a recalled *view‑point* happens to fall inside the ring. We therefore lead with soft‑SPL and report both success radii.

### 4.1 Why a controlled revisit harness (and not GOAT‑Bench)

A natural question is why we do not report on GOAT‑Bench [Khanna et al., CVPR 2024], the standard multi‑modal lifelong‑navigation benchmark on HM3D, in which an agent visits 5–10 sequential open‑vocabulary sub‑goals — specified by category, language, or image — within a single unseen‑scene episode (val_unseen: 36 scenes × 10 episodes = 360 episodes), scored by Success Rate and SPL at the 1.0 m radius. GOAT‑Bench is the closest benchmark to our thesis: its implicit‑memory analysis already shows that maintaining scene memory across sub‑goals improves efficiency (its explicit‑map Modular GOAT agent gains ≈6.6–10.9% SPL over the memoryless baselines; current SoTA SSMG‑Nav reports 0.341 SPL versus a memoryless VLFM's 0.159). We make three points. **First**, our cross‑visit revisit harness exercises the same underlying phenomenon GOAT's memory analysis targets — does a prior observation of a region help the agent return to it? — but in a deliberately more controlled and statistically tighter form: a paired cold‑map/warm‑revisit design with a per‑episode S1/S2/S3 module decomposition and 90% bootstrap confidence intervals, where GOAT‑Bench reports aggregate point estimates with no CIs or seeds. This lets us attribute the gain to the long‑term memory specifically (S2−S1 ≈ 0, S3−S2 carries the effect) and to separate a warm relevant‑recall regime from a cold incidental one — a separation GOAT's within‑episode protocol entangles. **Second**, GOAT‑Bench is strictly broader than our study along two axes we deliberately do not claim: open‑vocabulary image‑ and language‑instance goals, and explicit instance disambiguation. Both fall on exactly the bottleneck we measure — our caption embedding cannot discriminate object instances (within‑instance cosine 0.628 vs between‑instance‑same‑category 0.535, collapsed by the category query to a 0.047 rank gap) — so a GOAT image‑goal episode would test instance grounding our ReMEmbR backbone does not provide, conflating a perception limitation with the memory question we isolate. **Third**, on the comparable axis our numbers are reported in GOAT's own convention: at the standard 1.0 m success radius our warm success rises from 0.33 to 0.67, the directly comparable benchmark figure (we relegate the strict 0.1 m radius to a localization‑bound diagnostic). We therefore treat GOAT‑Bench as the canonical lifelong benchmark our controlled harness *decomposes* rather than *replaces*; a full GOAT‑Bench evaluation — adding image‑ and language‑goal grounding and instance‑level perception to the backbone — is the natural next step once the instance‑discrimination bottleneck this paper identifies is addressed, and is a concrete instance of the recall‑rewarding evaluation we argue the next gain requires.

### 4.2 Baselines and external comparison (VLFM)

Our primary comparator is the controlled, same‑backbone memory ablation S1/S2/S3, which isolates the LTM's contribution as a paired warm‑vs‑cold delta. We deliberately do not report an absolute success‑rate horse‑race against an external system, and we explain why with reference to the closest external method, **VLFM** [Yokoyama et al., ICRA 2024; arXiv:2312.03275]. VLFM is the de‑facto zero‑shot modular ObjectNav baseline on HM3D (val SR 52.5%, SPL 30.4% at the 1.0 m ring) and shares our system's core perceptual mechanism: a vision‑language **category cosine** (BLIP‑2 image–text similarity between an RGB observation and a goal prompt) used to score where to go. The decisive difference is memory: VLFM grounds that cosine into a **per‑episode** value map over frontiers and discards it at episode end — it has no persistent, cross‑visit LTM — whereas our contribution is precisely the persistent consolidated LTM and its memory‑injected re‑ranking. VLFM is therefore the right reference point but the wrong scoreboard. First, our headline is a *paired* warm‑vs‑cold soft‑SPL delta on a single fixed backbone with bootstrap CIs; an absolute SR from VLFM — produced by a different code stack (a separately‑trained 2.5‑billion‑step PointNav policy plus a four‑model VLM server, against an earlier habitat‑lab version) — cannot serve as a controlled comparator for a *memory delta*, and treating it as one would substitute an apples‑to‑oranges gap for the controlled effect we measure. Second, our absolute success rate is below VLFM‑class systems because the 2B/7B ReMEmbR backbone's exploration ceiling starves long episodes, **not** because of the memory; since memory is injected as waypoint candidates into the planner's pool, the mechanism is planner‑independent and the recall delta would, if anything, compose with a stronger explorer. We keep our internal baseline honest about this: **S1 is memory‑off ReMEmbR — an internal frontier‑and‑caption baseline, not a re‑implementation of VLFM**, and we never claim otherwise. Third, VLFM is in fact *evidence for* our thesis: its strong zero‑shot results confirm that a category‑level cosine is discriminative **at the category level** — exactly the regime where our memory also works — and throw into relief our measured failure at the **instance** level (the same cosine collapses same‑category instances to a 0.047 rank gap). Finally, on the lifelong multi‑goal regime where memory is designed to pay off, VLFM's memoryless value map falls to 0.159 SPL on GOAT‑Bench (vs 0.341 for a persistent consolidating memory), corroborating our boundary result that memory's value appears under *relevant revisits*, not single‑shot search. We therefore position VLFM as the published absolute‑performance bar and the closest external relative of our frontier‑and‑caption baseline, and frame our contribution as a **memory delta on a controlled internal baseline**, not an absolute‑SOTA claim. (A VLFM‑style per‑frontier value‑map bonus on our own frontier backbone is an optional in‑harness, same‑backbone comparator we leave to future work; it would remain an *internal* baseline, not a faithful VLFM run.)

### 4.3 Statistical methodology and power

We report graded soft‑SPL as a warm paired delta S3−S1 over visits matched on `(scene, target‑category, visit‑order)`, with 90% confidence intervals from a paired percentile bootstrap (5,000 resamples, fixed seed) and a one‑sided bootstrap p‑value for the null delta ≤ 0. To address small‑sample reliability in the spirit of rliable [Agarwal et al., NeurIPS 2021], we additionally report two robust statistics on the *same* per‑pair delta distribution (implemented as an opt‑in `--power` mode of the analyzer; `embodied_memory/scripts/analyze_revisit.py`). **(i)** The **interquartile mean (IQM)** — the mean of the central 50% of the paired deltas — which is insensitive to a few outlier pairs; for the wide matrix it agrees with the mean [verify: IQM vs mean +0.23, n=26], confirming the effect is not driven by one or two episodes. **(ii)** The **probability of improvement** P(S3>S1), the fraction of warm pairs in which the full system beats the memory‑off baseline (ties 0.5); it is [verify] on warm visits versus ≈0.5 on the cold control — memory helps in roughly five of six warm pairs and is a coin flip when no prior sighting exists. We do **not** report rliable performance profiles or stratified across‑task bootstrap CIs: those are defined over a *distribution of tasks or algorithms*, whereas ours is a single task and the appropriate stratification is the same‑pair pairing we already use. Finally, a **power accounting** answers whether n=26 is adequate. With the realized paired‑delta standard deviation (σ ≈ [verify ~0.25] soft‑SPL), the minimum effect detectable at one‑sided p<0.05 is ≈ z₍.05₎·σ/√n = [verify ~0.08] at n=26, and the effect detectable with 80% power is (z₍.05₎+z₍.20₎)·σ/√n ≈ [verify ~0.12]; the observed +0.23 is about twice this minimum‑detectable‑effect‑size, so the warm result is adequately powered. The same accounting bounds our smaller samples honestly: the n=12 chair+bed subset resolves ≈0.18 at 80% power (so its +0.24 sits at the edge of adequate power, hence its wider CI), and the n=2 audio‑write probe has an 80%‑power MDES of ≈0.44 — larger than any effect we could plausibly observe — which is exactly why we report that probe as underpowered and inconclusive (p=0.48) rather than as evidence. These normal‑approximation thresholds are *planning heuristics*; the reported significance throughout comes from the paired bootstrap, not the approximation. (Exact IQM / P(improve) / σ / MDES are regenerated from the wide‑matrix run deltas via `analyze_revisit.py --power` and are marked [verify] until the run dirs are quoted.)

---

## 5. Results

### 5.1 Memory helps when recall is relevant (warm revisit)

On the warm revisit, the full system improves paired soft‑SPL over the memory‑off baseline. The headline is the **warm‑paired** S3−S1 delta on the renumbering‑invariant key `(scene, category, visit_order)` — a genuine paired delta, not a difference of pooled means:

| Dataset | n (warm pairs) | warm soft‑SPL S3−S1 | 90% CI | p | SR@1.0 m (S1→S3) |
|---|---|---|---|---|---|
| **Wide, 6 categories × 2 scenes (corrected)** | **26** | **+0.230** | [+0.129, +0.324] | **<0.001** | 0.154 → 0.423 |
| Phase‑C (chair+bed, 2 scenes) [pre‑correction] | 12 | +0.240 | [+0.073, +0.417] | 0.008 | 0.333 → 0.667 |

**Methodology correction (shuffle fix).** The wide estimate is a *fresh re‑run* after we found and fixed an episode‑ordering bug: the Habitat episode‑iterator pin targeted a non‑existent config key, so it silently no‑op'd and the iterator's default **shuffle stayed on** in every earlier run — diluting the warm effect because some "warm" revisits were processed before their cold seed was consolidated (a clean cold control bounds the damage; the pairing itself stays valid because the shuffle is identically seeded across S1/S3). Fixing it roughly **doubled** the measured wide effect (**+0.115 → +0.230**, n=26). We state this as a *cross‑run* methodology correction, **not** a paired before/after test (the two values are separate runs, each with its own CI; there is no test on the difference of deltas) and **not** a new reproduction. The corrected wide value now agrees with the n=12 chair+bed subset (+0.240). The Phase‑C (+0.240) and audio (§5.3, +0.171) numbers are **pre‑correction** and are therefore conservative lower bounds pending re‑run. Binary SPL@0.1 m for the corrected wide is +0.044 (p=0.105, not significant — localization‑bound). Treating reruns and re‑analyses as one, the result rests on **≈2–3 distinct datasets**, not "~12 reproductions."

### 5.2 The gain is LTM‑specific (module decomposition)

Adding *only* the short‑term buffer does nothing; the gain appears only with consolidation + LTM + re‑ranking. The warm‑paired decomposition (corrected wide run):

| Contrast (warm‑paired) | Wide (n=26, corrected) | Phase‑C (n=12) |
|---|---|---|
| S2 − S1 (STM only) | **−0.009** (n.s., p=1.00) | +0.000 |
| S3 − S2 (consolidation+LTM+rerank) | **+0.238** [+0.135, +0.335] (p<0.001) | +0.240 |

So essentially **100% of the soft‑SPL effect is attributable to the LTM, not the short‑term memory** — the STM‑only setting captures none of it, and the full effect lands in consolidation + hierarchical‑LTM + re‑ranking (proposal modules 2–4). Memory fired on 25/26 warm visits; warm 1.0 m success rose 0.154 → 0.423.

### 5.3 The effect reproduces in the audio task

On the AudioGoal anomaly‑response task (2 scenes × 3 anomaly/object cells, S1/S2/S3), the warm gain reproduces independently:

- Pooled warm soft‑SPL **S3−S1 = +0.171** (n=18, 90% CI [+0.070, +0.277], p=0.002) — measured **pre‑shuffle‑fix**, so a conservative lower bound pending re‑run under the corrected ordering (§5.1).
- Clean decomposition: S2−S1 not significant ⇒ the LTM‑specific S3−S2 = **+0.172** (p=0.004).
- Cold control ≈ 0 (memory inert without a prior sighting).
- **First significant strict‑radius success gain:** binary SPL@0.1 m **+0.139** (p=0.003), where the cold seed begins at a goal view‑point so the 0.1 m ring is occasionally reachable.

We flag **high heterogeneity**: 4/6 cells win, 2 regress; a leave‑best‑cell‑out estimate softens to +0.095 (p≈0.07). The gain here is **visual** recall — the audio serves as onset trigger and (when gated) causal switch, not as a localizer (§5.6).

### 5.4 Memory is neutral when observation is incidental (cold MultiON)

On cold K=3 chains, where the agent only sees objects in passing while hunting other goals, memory does **nothing**:

| | S1 | S2 | S3 |
|---|---|---|---|
| Progress (n=14/setting) | 0.190 | 0.190 | 0.190 |

S3−S1 = +0.0000; the gap by sub‑goal index is identical at every index — **zero compounding**. The cause is twofold: the backbone's exploration ceiling starves the premise (the agent rarely survives to the recall moment — found‑rate at sub‑goal 0 is 0.5, at index 1 only 0.07), and incidental cold sightings are not warm relevant priors (the caption embedding cannot tell instances apart). The net statement is sharper than either result alone: **the LTM helps when past observation is relevant, and is cleanly neutral when it is incidental.**

### 5.5 What does *not* help: five inert heads and one redundant write

We added six separately‑motivated memory mechanisms. Each was built, made correct, instrumented, and ablated; each is inert or redundant:

| Mechanism | Outcome |
|---|---|
| **Trained importance head** (R, learned on a per‑keyframe goal‑object label) | Recovers to *heuristic‑competitive* on warm (+0.194 vs +0.236, statistical tie) but does **not beat** the hand‑tuned heuristic and hurts cold; head‑to‑head at scale is parity. |
| **Trained utility / forward‑model head** (U, self‑supervised next‑caption surprise) | **Regresses** warm (+0.112 → +0.061) via an over‑fire signature; three U formulations all land at ≈+0.06 with the same over‑fire. |
| **Coarse room‑affordance prior** (CLIP zero‑shot room classifier + category→room prior) | Built, CLIP‑grounded, *proposes* but is **never chosen** at the re‑rank weight; it always loses to concrete sightings. Zero over‑fire, zero gain. |
| **Temporal‑recency head** (M4, additive recency bonus) | **Honest negative:** does not change warm outcomes (B−A = −0.0005, a tie at the floor); fires more but the extra picks are credit re‑attribution, not re‑routing. |
| **Audio direction‑of‑arrival re‑ranker** (S2 head, lateral‑sign bonus) | **Structural negative:** its zero‑sum, mean‑centered bonus is provably 0.0 on a single recalled instance — there is nothing to disambiguate. |
| **Write the heard anomaly to LTM** (Step 2, store source location) | **Mechanism‑verified** (the write fires and is recalled at a distance). On a **line‑of‑sight** seed it is redundant with visual recall *by construction* (the cold seed is ≈0.5 m line‑of‑sight to the source, so vision already maps it and write‑OFF already routes there; write‑ON vs write‑OFF ≈ −0.012 once an over‑fire confound is damped — a tie). On a **controlled non‑line‑of‑sight‑but‑audible** seed (placed via a captioner‑gated occlusion check so vision provably cannot map the source) the write shows the **first non‑redundant direction** (write‑ON 0.213 vs write‑OFF 0.144, **+0.068**) — but at **n=2 recall pairs it is underpowered and inconclusive** (90% CI [0.000, +0.137], p=0.48), and the write stamps the **ground‑truth source location** (an oracle upper bound). So: directional and mechanism‑verified, not yet evidence that audio writes help. |

**One cause unifies them.** We measured the caption embedding's instance discrimination directly: within‑instance caption cosine **0.628** vs between‑instance‑same‑category **0.535** — a real **+0.093** separation — but the live category query `"there is a {category}"` collapses instances to a **0.047** rank gap. Every head above either (a) stores or re‑weights *more* goal‑ish frames, which under this ceiling surfaces more *wrong‑instance* candidates → over‑retrieval → worse arrival (the importance/utility heads, the anomaly write), or (b) tries to disambiguate instances that the **single‑goal** evaluation never forces a choice among (the affordance, temporal, and DOA heads). The hand‑tuned, conservative heuristic wins precisely because conservatism is the right bias in an instance‑ambiguous space.

### 5.6 Audio: necessary and detectable, but the gain is visual

We verify three things about the audio path. **(a) Open‑set detection works:** the CLAP contrastive‑prompt gate separates anomaly clips (alarm/glass/cry) from benign household sound (footsteps/coughing/knock/vacuum) with a clean margin — equal‑error‑rate **0.00** on our staged clips, recommended decision margin 0.137. **(b) The onset is causally necessary:** with the onset‑gate enabled, suppressing the sound suppresses the recall (no onset → no memory injection → no warm gain). **(c) But localization is not the source of the gain:** the simulator's binaural cue is inter‑aural‑level only (the time‑difference cue is near‑zero by construction), giving a left/right *sign* but no range; consequently the DOA re‑ranker is inert (§5.5) and the +0.171 warm gain is **visual** recall with audio as trigger and gate. Writing the source location into memory is mechanism‑verified but redundant *by construction* on a line‑of‑sight seed (vision already maps the source and write‑OFF already routes there). A controlled non‑line‑of‑sight‑but‑audible seed breaks that redundancy and gives the first non‑redundant *direction*, but it is underpowered and still oracle‑sourced — directional only; see §5.5 (results) and §7 (the bounded probe) for the numbers.

---

## 6. Analysis: the boundary of LTM utility

Putting the results together, the utility of this LTM is governed by two axes:

1. **Relevance of recall.** Memory helps iff a past observation is *relevant* to the current goal and *navigable‑to* (a stored view‑point). Warm revisits satisfy this (+0.23 at n=26 corrected; +0.24 at n=12); cold/incidental sightings do not (≈0).
2. **Whether the task rewards instance disambiguation.** Every mechanism that could only pay off by choosing the *right* same‑category instance is inert, because (i) the single‑goal evaluation never forces that choice and (ii) the caption embedding cannot make it anyway.

This yields a concrete prediction for where the next gain lies: **not** in additional memory heads, but in **instance‑level perception** (a better instance‑discriminating embedding or a trained detector) and/or an **evaluation that rewards recall** — multi‑instance, changed‑world, or genuinely cross‑environment episodes. We tested the cross‑environment hypothesis directly and found it **structurally absent**: the injector hard‑filters to the current scene, so a home sighting is *recalled* in a new scene (counter > 0) but never *injected* as a waypoint; freezing away‑scene writes collapses the apparent transfer to ≈0 with zero memory chosen. Positive cross‑environment transfer therefore requires a position‑free mechanism (a working coarse‑affordance layer) — which, as §5.5 shows, the current re‑ranker declines to use.

**A $0 pre‑screen says the cheapest instance‑level lever is query construction — and it clears a pre‑registered bar.** Before any detector, the retrieval *query* is the obvious first place to look: the instance signal *exists* in the embedding (+0.093 within‑vs‑between) but the bare category query `"there is a {category}"` discards it (§5.5). On a controlled instance corpus we A/B'd five deployable query constructions under one leave‑one‑out goal‑vs‑best‑distractor metric (offline, model‑only, $0). The live bare‑category query scores a pooled rank gap of **−0.039** — it carries *no* instance preference, ranking same‑category distractors slightly above the goal on average. Querying instead with the agent's own **prior‑sighting caption** lifts the pooled gap to **+0.051**, beating the baseline on chair (+0.131) and bed (+0.119) and clearing a pre‑registered bar (>0.02 on the pooled gap *and* on ≥2 categories); a richer *generic* template does **not** help, confirming the win comes from using prior sightings, not phrasing. The recovery is honestly **per‑category‑fragile** (sofa/toilet stay flat), so this is a directional pre‑screen on a hand‑built corpus, not yet a navigation result — but it names the concrete, deployable next experiment: a default‑OFF prior‑sighting / pseudo‑relevance‑feedback query in the injector, gated to a single A/B run. Critically this is a *query*‑side change, categorically different from the read‑side re‑rank heads of §5.5, which are zero‑sum and provably inert on a single recalled instance.

---

## 7. Limitations

- **Scope is within‑scene, same‑category recall**, not cross‑environment reuse; we verify the latter is structurally absent in the current injector.
- **"Hierarchical" is, in the measured path, fine‑layer + re‑ranking** (mid empty, coarse never chosen). We do not claim a working three‑layer hierarchy.
- **Headline magnitude is dataset‑dependent and was shuffle‑sensitive:** the corrected wide estimate is +0.23 at n=26 (up from a shuffle‑diluted +0.115; a single‑seed re‑run, not yet replicated), consistent with +0.24 at n=12; the audio reproduction (+0.171, pre‑correction) is heterogeneous across cells and softens to +0.095 leave‑best‑out.
- **Success radius matters:** strict 0.1 m success is perception‑bound; we lead with soft‑SPL and report 1.0 m success (0.33→0.67) as the comparable benchmark number.
- **The anomaly‑write result is a deliberately‑bounded probe, not a half‑finished feature.** It stamps an oracle (ground‑truth) source location and runs on a line‑of‑sight, single‑instance seed, which makes the redundancy with vision a property of the harness rather than of the mechanism. The genuine converting experiment is a *non‑line‑of‑sight‑but‑audible* seed (so vision cannot map the source and the audio write has a unique job), **multiple same‑category instances** (so recall has something to disambiguate), and a **range‑capable, non‑oracle direction‑of‑arrival write** (e.g., multi‑step triangulation or a learned range model). We verified that the first two ingredients are in tension on the current stack: the energy‑STOP termination gate forces the agent to *approach* the source to stop, which usually restores line of sight and collapses back to the redundant baseline; and the simulator's binaural cue is inter‑aural‑level only (the time‑difference cue is stripped by construction), giving a left/right *sign* with **no range**, so a guessed‑range write lands meters off and becomes a wrong‑side attractor — the same over‑fire family seen for the importance heads. We have since built the first ingredient — a captioner‑gated **non‑line‑of‑sight‑but‑audible** seed (verified that vision does not name the source object at the seed pose) — and a write‑ON/OFF A/B there gives the first non‑redundant direction (write‑ON 0.213 vs write‑OFF 0.144, **+0.068**), but it is **underpowered and inconclusive** (n=2 recall pairs, p=0.48) and still uses an **oracle** ground‑truth source location. The remaining ingredients — multiple same‑category instances, a range‑capable **non‑oracle** direction‑of‑arrival write, and a powered multi‑cell version (the current single cell yields only 2 recall pairs) — are left to future work; the oracle write is the honest upper bound we report.
- **Backbone variance and small n** (paired bootstrap, n=12–26) bound the precision of every estimate; we report CIs and leave‑one‑out throughout.

---

## 8. Conclusion

We built a faithful lifelong hierarchical LTM for an embodied agent and asked when it helps. The answer is precise and reproduced across object‑ and audio‑goal tasks: **memory helps when past observation is relevant to the current goal (+0.23 soft‑SPL at n=26 corrected, 0.33→0.67 success), is cleanly neutral when observation is incidental, and is not further improved by additional importance, affordance, temporal, direction‑of‑arrival, or anomaly‑write heads** — because the caption embedding cannot discriminate instances and the single‑goal evaluation never rewards doing so. For the anomaly‑response application we contribute a verified open‑set audio anomaly detector and a causal onset‑gate, while showing honestly that the navigation gain is visual recall, not audio localization. The practical takeaway for embodied‑memory research is a redirection: the next gain is in instance‑level perception and recall‑rewarding evaluation, not in more memory heads.

---

## Appendix A. Reproducibility

- **Backbone:** Qwen2‑VL‑2B (captioner) + Qwen2.5‑7B (planner); Habitat + HM3D `val_mini`.
- **Audio:** SoundSpaces binaural RIR grid rendered offline; O(1) `fftconvolve` at run time; real ESC‑50 anomaly + benign clips (CC BY‑NC).
- **Ablation:** `S∈{1,2,3}` via `embodied_memory/run_hm3d_pol.py --backbone remembr --setting S`; paired‑bootstrap analyzer `analyze_ablation.py [--revisit|--multion]`.
- **Statistics:** warm‑paired soft‑SPL with 90% bootstrap CIs; module decomposition S2−S1 / S3−S2; leave‑one‑cell‑out robustness; rliable‑style robustness + power via `analyze_revisit.py --power` (IQM, probability‑of‑improvement P(S3>S1), MDES/power note).
- **Negative‑result instrumentation:** per‑run counters (`n_memory_candidates`, `n_memory_chosen`, `n_audio_writes`, `n_audio_event_recalled`, write‑seam skip reasons) make every "inert head" claim a checkable number rather than an assertion.
- Code, drivers, and per‑run summaries: `embodied_memory/`, `scripts/race-*.sh`, `runs/*/summary.json`. Full development arc and adversarially‑checked caveats: `PHASE2_ABLATION_REPORT.md`.

## Appendix B. Numbers to finalize before submission [verify]

- ~~Confirm exact n, CI, p for the wide‑matrix +0.115~~ — **DONE: re‑run under the shuffle fix → +0.2296 (n=26, 90% CI [+0.129, +0.324], p<0.001); decomposition S2−S1 = −0.009 / S3−S2 = +0.238 (§5.1–5.2).** Remaining: **re‑run Phase‑C and the audio M3 matrix under the same fix** (both pre‑correction lower bounds), and **replicate the corrected wide at a second seed.**
- ~~Re‑verify the flagged 2026 arXiv ids~~ — **DONE (web, June 2026): all five resolve. Corrected two mis‑attributions — HomeEmergency is Mullen et al. (not "Yan et al."), arXiv:2504.01089; and arXiv:2601.21667 is Ju et al., "From Instruction to Event: Sound‑Triggered Mobile Manipulation" (the "Habitat‑Echo/STMM" name was wrong).** Remaining: confirm the TrajRAG *venue* (cited as arXiv:2605.01700; the "CVPR 2026" claim is unconfirmed) and the SSMG‑Nav venue.
- **rliable power block: code DONE + green** (`analyze_revisit.py --power`: IQM, P(S3>S1), MDES; 5 TDD cases). Remaining: regenerate the exact IQM / P(improve) / σ / MDES from the wide‑matrix run deltas (`analyze_revisit.py --power runs/revisit-shuffix-wide-s{1,2,3}`) and replace the §4.3 [verify] placeholders.
- **Query‑side instance fix — $0 Stage‑0 pre‑screen DONE + green** (`diagnose_sbert_cosines.py` `query_template_ab`): bare‑category pooled goal‑vs‑distractor rank gap **−0.039 → caption‑query +0.051** (clears the ≥2‑category bar on chair+bed; flat sofa/toilet). Optional next (user‑gated): wire a default‑OFF prior‑sighting/PRF query in `propose_memory_candidates` + one S3‑only RACE A/B (§6).
- Re‑state the MultiON found‑rate (0.5 / 0.07) and Progress (0.190) from `runs/` at submission time.
- Lock the audio M3 per‑cell table (4/6 win) and the leave‑best‑out +0.095/p≈0.07.
- Author list, affiliations, acknowledgements, dataset/clip licensing statement (ESC‑50 CC BY‑NC).
- Figures: (1) system diagram (STM→consolidation→fine/mid/coarse→rerank + audio two‑env split); (2) warm vs cold soft‑SPL bar with CIs; (3) the instance‑cosine separation (0.628/0.535/0.047) as the unifying‑bottleneck figure; (4) audio anomaly‑vs‑benign margin histogram (EER 0.00).
